export type articleprops = {
  action: string;
};
export type articleCard = {
  title: string;
  content: string;
  tags: string;
  campas: boolean;
  img: string;
  url: string | null;
  userid: string;
};
